export enum FormattingOption {
  BOLD = "bold",
  ITALIC = "italic",
  UNDERLINE = "underline",
  STRIKE = "strike",
}

export enum Script {
  SUPER = "super",
  SUB = "sub",
}

export enum List {
  BULLET = "bullet",
  ORDERED = "ordered",
}

export enum Attachment {
  IMAGE = "image",
  LINK = "link",
}
